## 🧩 **🟢 Basic Stack Practice Problems (Level 1 – Fundamentals)**

These will make you comfortable with push/pop/peek logic and dry-running.

1. **Implement Stack using Array**
   ➤ Write your own class `MyStack` with `push()`, `pop()`, `peek()`, and `isEmpty()` methods.

2. **Implement Stack using Linked List**

3. **Reverse a String using Stack**
   ➤ Input: `"abcd"` → Output: `"dcba"`

4. **Check for Balanced Parentheses**
   ➤ Input: `"({[]})"` → Output: `true`
   ➤ Input: `"([)]"` → Output: `false`

5. **Delete Middle Element of a Stack (without using extra stack)**
   ➤ Input: `[1,2,3,4,5]` → Output: `[1,2,4,5]`

6. **Insert an Element at the Bottom of Stack (Recursion allowed)**
   ➤ Input stack top→ `[5,4,3,2,1]`, insert `10`
   ➤ Output: top→ `[10,5,4,3,2,1]`
